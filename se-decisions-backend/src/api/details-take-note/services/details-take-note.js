'use strict';

/**
 * details-take-note service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::details-take-note.details-take-note');
