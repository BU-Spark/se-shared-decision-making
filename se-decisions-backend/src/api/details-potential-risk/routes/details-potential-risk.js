'use strict';

/**
 * details-potential-risk router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::details-potential-risk.details-potential-risk');
